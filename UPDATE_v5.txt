v5 原因対策版
1. 旧背景は上半分だけ実景で下半分をぼかし延長していたため、13枚すべて廃止。
2. 全画面の高品質縦長シネマティック素材から13枚を再生成。ぼかし延長なし。
3. 背景に残る旧UIは不透明なライブHUDで完全に覆う。
4. 簡易CSS人形を削除。背景の高品質人物を使用。
5. 門選択時はカメラが左右の門へ寄る演出に変更。
6. 正解色は成功/失敗を問わず毎試行、直近13件まで保存。
7. 統計は通常画面の下部に置かず、右上ボタンから別画面のみ。
8. Service Worker v5で旧キャッシュを全削除し、CSS/JS/背景にバージョンを付与。
